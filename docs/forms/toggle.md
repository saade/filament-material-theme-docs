# Toggle

<Shot name="switch/on" alt="A switch in its on state, with the handle grown and the track filled" />

<Shot name="switch/off" alt="A switch in its off state, with a smaller handle on a gray track" />

```php
use Filament\Forms\Components\Toggle;

Toggle::make('notifications');
```
